<%= form_with(model: wallets_transfer) do |form| %>
  <% if wallets_transfer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wallets_transfer.errors.count, "error") %> prohibited this wallets_transfer from being saved:</h2>

      <ul>
        <% wallets_transfer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
